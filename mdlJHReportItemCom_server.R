
mdlJHReportItemServer::TestRecordServer(input = input,output = output,session = session,dms_token=dms_token,erp_token = erp_token)

mdlJHReportItemServer::TemplateListServer(input = input,output = output,session = session,dms_token=dms_token,erp_token = erp_token)

mdlJHReportItemServer::ProductQualityServer(input = input,output = output,session = session,dms_token=dms_token,erp_token = erp_token)

mdlJHReportItemServer::CustomerMaterialCorrespondenceServer(input = input,output = output,session = session,dms_token=dms_token,erp_token = erp_token)

mdlJHReportItemServer::CustomerTemplatesServer(input = input,output = output,session = session,dms_token=dms_token,erp_token = erp_token)


